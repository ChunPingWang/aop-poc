package com.example.contact.domain.model;

/**
 * Enum representing the type of operation performed on the system.
 * Used for audit logging purposes.
 */
public enum OperationType {
    CREATE,
    READ,
    UPDATE,
    DELETE
}
